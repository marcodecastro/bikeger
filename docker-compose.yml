services:
  mongo:
    image: mongo:7
    command: ["--replSet", "rs0", "--bind_ip_all"]
    # Notebook: só localhost. Em VPS/produção apague o bloco ports —
    # a API no mesmo compose fala com o hostname `mongo`.
    # Produção: a API recusa subir se este Mongo não for replica set (rs0).
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - bikeger_mongo:/data/db
    healthcheck:
      test: >
        mongosh --quiet --eval
        "try { rs.status().ok } catch (e) { rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: '127.0.0.1:27017' }] }).ok }"
      interval: 5s
      timeout: 15s
      retries: 20
      start_period: 10s

volumes:
  bikeger_mongo:
