# URL pública da API. O Vite embute isso no JavaScript do painel — não é secret,
# mas cada deploy precisa deste valor alinhado com o Render.
# Copie para .env.production se o arquivo não existir.
VITE_API_URL=https://bikeger.onrender.com
